# Project-level npm config for the www/ Astro site.
# The Microsoft proxy registry returns packument metadata with .tgz download
# URLs pointing to the upstream Azure DevOps feed. The user-level npmrc sets
# allow-remote=none which blocks those fetches. Override here to allow them
# for this workspace only so Astro's dependencies can be resolved.
allow-remote=all
